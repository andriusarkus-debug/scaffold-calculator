package com.scaffold.model.enums;

// Namo formos tipas — nuo to priklauso returnų skaičius ir perimetro skaičiavimas
public enum HouseShape {
    RECTANGULAR, // Paprastas stačiakampis — 4 kampai (returnai)
    L_SHAPE      // L formos namas — 6 kampai (returnai)
}
